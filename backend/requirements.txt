# ── Web framework (the app now runs on this, not Streamlit) ────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9   # required for FastAPI to accept file uploads

# ── Auth (new — did not exist in the original requirements.txt) ────────────
passlib[bcrypt]==1.7.4
pyjwt==2.9.0

# ── Config ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Data / DB ────────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
sqlalchemy==2.0.32
openpyxl==3.1.5

# ── OCR pipeline ─────────────────────────────────────────────────────────
pillow==10.4.0
opencv-python-headless==4.10.0.84
pytesseract==0.3.13
pdf2image==1.17.0
paddlepaddle==2.6.2
paddleocr==2.8.1

# ── Reports / misc (kept from original, used by other parts of the app) ──
reportlab==4.2.2
plotly==5.24.0
